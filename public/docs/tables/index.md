# Tables

Status: mixed

Available: one D1 database per app, ordered local and remote SQL migrations, stable bindings, and database inspection.

Never edit an applied migration. Add the next numbered SQL file.

Planned: typed schema helpers, data browser, dump, export, restore, preview databases, and Door-aware authorization.
