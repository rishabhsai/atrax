# Tables

Status: mixed

Available: one D1 database per app, ordered local and remote SQL migrations, stable bindings, database inspection, and `atrax tables export [--out <file>]`, which dumps every user table as JSON on both the Cloudflare-account and instant paths.

Never edit an applied migration. Add the next numbered SQL file.

Planned: typed schema helpers, data browser, restore, preview databases, and Door-aware authorization.
