# Triage labels

Use the default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`.

The user approved `ready-for-agent` for the launch tickets. Labels describe readiness; native blocking relationships determine which tickets can start.
